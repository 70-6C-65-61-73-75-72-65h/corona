export const virusState = {
  cases: null,
  deaths: null,
  recovered: null,
};

export const virusHistoryFields = Object.keys(virusState);

export const articleState = {
  url: "",
  source: "",
  title: "",
};

export const articleFields = Object.keys(articleState);
